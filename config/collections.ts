// Top-level collection titles in display order (most general first).
// Used by the breadcrumb to determine parent vs child collection.
// To add a new top-level category, append its title here — no other code changes needed.
// See docs/decisions/008-breadcrumb-collection-hierarchy.md
export const topLevelCollections = ['Women', 'Men', 'New In'];
