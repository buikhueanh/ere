// Stub — real Journal (Shopify blog articles) lands in renovation Phase 7.
// Exists now so the new nav doesn't 404.
export default function JournalPage() {
  return (
    <div className="min-h-[50vh] flex items-center justify-center">
      <p className="text-sm text-muted">journal — coming with the renovation</p>
    </div>
  );
}
