// Stub — real New In grid (4-up + pagination + newsletter module) lands in
// renovation Phase 4. Exists now so the new nav doesn't 404.
export default function NewInPage() {
  return (
    <div className="min-h-[50vh] flex items-center justify-center">
      <p className="text-sm text-muted">new in — coming with the renovation</p>
    </div>
  );
}
